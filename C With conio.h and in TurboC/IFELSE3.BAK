#include<stdio.h>
#include<conio.h>
void main()
{       int a,b,c;
	clrscr();
		printf("Enter 1st no :");
		scanf("%d",&a);
		printf("Enter 2nd no :");
		scanf("%d",&b);
		printf("Enter 3rd no :");
		scanf("%d",&c);

			if (a>b && a>c){
				printf("no1 is Greater");
			}
			else if(b>a && b>c){
				printf("no2 is Greater");
			}
			else {
				printf("no3 is Greater");
			}


	getch();
}
